# Runtime / local instance data (lives only in the CurseForge folder)
saves/
logs/
.crash-reports/
.local/
local/
downloads/
.cache/

# Large binaries — track mod list separately, not jars
mods/*.jar

# Generated at runtime by Random One Block reload (regenerate via /randomblockreload)
kubejs/config/random_one_block_pool.json
kubejs/config/random_one_block_pool.txt
kubejs/config/random_one_block_mod_pools_debug.json
kubejs/config/random_one_block_unlocks/

# Exported branding raster (keep SVG source in git)
branding/logo/logo.png

# CurseForge export artifacts
dist/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~